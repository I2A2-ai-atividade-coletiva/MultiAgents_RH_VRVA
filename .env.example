# Chaves e configuração do modelo
# Copie este arquivo para `.env` e preencha os valores.

# Google Gemini
GOOGLE_API_KEY=
GENAI_MODEL=gemini-2.5-pro
GENAI_TEMPERATURE=0.2

# Groq (Modelos open LLM acelerados)
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-70b-versatile

# Provedor de LLM: google | groq
LLM_PROVIDER=google

# Ajustes gerais
#TIMEOUT_SECONDS=120

# -------------------------------
# Cálculo VR/VA e Regras
# -------------------------------
# Base de valores para cálculo:
#  - CCT (usa regras extraídas e LLM como fallback)
#  - MANUAL (usa planilha/base manual quando disponível)
VRVA_VAL_BASE=CCT

# Dias fixos por UF (fallback):
DIAS_FIXOS_SP=22
DIAS_FIXOS_RJ=21

# Jornada padrão (horas/dia):
JORNADA_PADRAO_HORAS=8

# -------------------------------
# OCR / Tesseract
# -------------------------------
# O ingest_ccts.py utiliza Tesseract OCR como fallback para PDFs em imagem.
# É necessário instalar no sistema:
#   sudo apt-get update && sudo apt-get install -y tesseract-ocr tesseract-ocr-por
